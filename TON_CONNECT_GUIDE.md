# TON Connect Integration Guide

## Что такое TON Connect?

TON Connect - это протокол для подключения TON кошельков к децентрализованным приложениям (dApps). Он позволяет пользователям один раз подключить свой кошелек и затем оплачивать участие в розыгрышах в один клик.

## Преимущества TON Connect

✅ **Лучший UX**: Кошелек подключается один раз, больше не нужно копировать адреса и комментарии
✅ **Автоматическая отправка**: Бот инициирует транзакцию, кошелек автоматически открывается для подтверждения
✅ **Безопасность**: Все транзакции подписываются в кошельке пользователя
✅ **Стандарт**: Поддерживается всеми крупными кошельками (Tonkeeper, MyTonWallet, OpenMask)
✅ **Персистентность**: Один раз подключил - работает постоянно

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                 ВАРИАНТЫ ОПЛАТЫ TON                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. TON Connect (НОВЫЙ - лучший UX)                         │
│     └─> Кошелек подключен → Бот инициирует транзакцию →    │
│         → Кошелек автоматически открывается для подписи     │
│                                                              │
│  2. Deep Links (СУЩЕСТВУЮЩИЙ - запасной вариант)            │
│     └─> Deep link с данными → Ручная отправка →            │
│         → Monitor отслеживает по комментарию                │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Установка

### 1. Зависимости

Уже добавлено в `requirements.txt`:
```
pytonconnect  # TON Connect SDK for wallet integration
```

Установите:
```bash
pip install -r requirements.txt
```

### 2. Конфигурация

Добавьте в `.env`:
```env
TON_CONNECT_MANIFEST_URL=https://your-domain.com/tonconnect-manifest.json
```

**ВАЖНО**: Файл `tonconnect-manifest.json` должен быть доступен публично по HTTPS.

### 3. Размещение Manifest файла

У вас есть несколько вариантов:

#### Вариант 1: GitHub Pages (бесплатно)
1. Создайте репозиторий на GitHub
2. Загрузите `tonconnect-manifest.json` в корень
3. Включите GitHub Pages в настройках
4. URL будет: `https://username.github.io/repo/tonconnect-manifest.json`

#### Вариант 2: Ваш сервер
1. Разместите файл в статической директории
2. Настройте Nginx/Apache для обслуживания статики
3. URL будет: `https://yourdomain.com/tonconnect-manifest.json`

#### Вариант 3: CDN (Cloudflare, etc.)
1. Загрузите файл на CDN
2. Получите публичный URL

### 4. Обновление Manifest

Отредактируйте `tonconnect-manifest.json`:
```json
{
  "url": "https://t.me/YOUR_BOT_USERNAME",
  "name": "Raffle Bot",
  "iconUrl": "https://your-domain.com/icon.png",
  "termsOfUseUrl": "https://t.me/YOUR_BOT_USERNAME",
  "privacyPolicyUrl": "https://t.me/YOUR_BOT_USERNAME"
}
```

Замените:
- `YOUR_BOT_USERNAME` - username вашего бота
- `iconUrl` - URL иконки вашего бота (PNG, 180x180px)

## Использование

### Для пользователей

#### Первое подключение:

1. Пользователь → "💎 Оплатить TON"
2. Бот показывает 2 варианта:
   - "🔗 Подключить кошелек" (TON Connect) ← **РЕКОМЕНДУЕМ**
   - "📋 Оплатить без подключения" (Deep Link)
3. Если выбрал TON Connect:
   - Показывается QR код для подключения
   - Кошелек подключается
   - Адрес сохраняется в БД (по `telegram_id`)

#### Последующие оплаты:

1. Пользователь → "💎 Оплатить TON"
2. Бот инициирует транзакцию через TON Connect
3. Кошелек автоматически открывается с готовой транзакцией
4. Пользователь только подтверждает
5. После подписи - транзакция отправляется
6. Monitor отслеживает и регистрирует участие

### Для разработчиков

#### Подключение кошелька:
```python
from app.services.ton_connect_service import ton_connect_service

# Генерация URL для подключения
connection_data = await ton_connect_service.get_connection_url(
    user_id=user.id,
    wallet_name="tonkeeper"
)

# Ожидание подключения (polling)
wallet_info = await ton_connect_service.listen_for_connection(
    user_id=user.id,
    timeout=300  # 5 минут
)
```

#### Отправка транзакции:
```python
# Отправка через подключенный кошелек
result = await ton_connect_service.send_transaction(
    user_id=user.id,
    destination=settings.TON_WALLET_ADDRESS,
    amount_nano=int(0.5 * 1_000_000_000),  # 0.5 TON
    comment="raffle_123_user_456"
)
```

#### Проверка подключения:
```python
# Проверить статус подключения
wallet_info = await ton_connect_service.check_connection(user_id=user.id)

if wallet_info:
    print(f"Connected: {wallet_info['address']}")
else:
    print("Not connected")
```

## База данных

Новая таблица `ton_connect_sessions`:

```python
class TonConnectSession(Base):
    __tablename__ = "ton_connect_sessions"

    id = Column(Integer, primary_key=True)
    user_id = Column(Integer, ForeignKey("users.id"))  # Связь с telegram_id через users
    wallet_address = Column(String(48))  # Подключенный адрес
    session_data = Column(JSON)  # Данные сессии TON Connect
    is_active = Column(Boolean, default=True)
    connected_at = Column(DateTime)
    last_used = Column(DateTime)
```

**Важно**: Идентификация пользователя идет через `telegram_id` → `users.id` → `ton_connect_sessions.user_id`.

## Handlers

### Новые обработчики в `app/handlers/ton_connect.py`:

- `connect_ton_wallet` - Начало подключения кошелька
- `disconnect_ton_wallet` - Отключение кошелька
- `check_ton_connection` - Проверка статуса подключения
- `ton_wallet_info` - Информация о подключенном кошельке

### Обновленные обработчики в `app/handlers/payment.py`:

- `pay_ton` - Теперь показывает выбор между TON Connect и Deep Links
- `pay_ton_connect_{raffle_id}` - Оплата через TON Connect
- `connect_and_pay_ton` - Подключение и оплата

## Клавиатуры

### Новые функции в `app/keyboards/inline.py`:

- `ton_connect_keyboard()` - Управление подключением кошелька
- `ton_payment_choice_keyboard()` - Выбор метода оплаты
- Обновлена `balance_keyboard()` - добавлена кнопка "💎 Указать TON кошелек"

## Безопасность

### Redis для хранения сессий

TON Connect использует Redis для хранения данных сессий:
- Ключи: `tonconnect:user:{user_id}:*`
- Автоматическое истечение (TTL) не установлено - сессии живут постоянно
- Данные зашифрованы внутри TON Connect SDK

### Проверки безопасности

1. **Валидация пользователя**: Все операции проверяют `telegram_id`
2. **Проверка активности сессии**: Только активные сессии используются
3. **Таймауты**: Подключение ограничено 5 минутами
4. **Логирование**: Все операции логируются

## Troubleshooting

### Manifest недоступен

**Ошибка**: `Failed to generate connection URL: Failed to fetch manifest`

**Решение**:
1. Проверьте, что `TON_CONNECT_MANIFEST_URL` указывает на публичный HTTPS URL
2. Откройте URL в браузере - файл должен отдаваться с `Content-Type: application/json`
3. Проверьте CORS headers (должны разрешать все источники)

### Кошелек не подключается

**Проблема**: Пользователь сканирует QR код, но подключения не происходит

**Решение**:
1. Проверьте логи: `grep "TON Connect" logs/bot_*.log`
2. Убедитесь, что Redis работает
3. Проверьте, что пользователь использует поддерживаемый кошелек (Tonkeeper, MyTonWallet)

### Транзакция не отправляется

**Проблема**: После подключения кошелька транзакция не инициируется

**Решение**:
1. Проверьте баланс кошелька пользователя
2. Убедитесь, что сессия активна: `SELECT * FROM ton_connect_sessions WHERE is_active = true`
3. Проверьте логи на наличие `TonConnectError`

## Мониторинг

### Логи

Все операции TON Connect логируются:
```
[INFO] TON Connect service initialized (manifest: https://...)
[INFO] Generated TON Connect URL for user 123, wallet: Tonkeeper
[INFO] Wallet connected for user 123: UQxx...
[INFO] Transaction sent via TON Connect for user 123, raffle 5, amount: 0.5 TON
```

### Метрики

Рекомендуемые метрики для мониторинга:
- Количество подключенных кошельков
- Процент успешных транзакций через TON Connect vs Deep Links
- Среднее время подключения кошелька
- Частота отключений

## Обратная совместимость

TON Connect **НЕ** заменяет существующий метод Deep Links.

Оба метода работают параллельно:
- **TON Connect** - для пользователей с подключенным кошельком (лучший UX)
- **Deep Links** - для новых пользователей или когда TON Connect недоступен

Это обеспечивает 100% доступность сервиса.

## Миграция существующих пользователей

Существующие пользователи продолжат использовать Deep Links.

Чтобы мотивировать их подключить кошелек:
1. Добавьте уведомление при оплате: "💡 Подключите кошелек для оплаты в 1 клик!"
2. Добавьте кнопку в профиль: "🔗 Подключить TON кошелек"
3. Показывайте преимущества TON Connect

## FAQ

### Q: Могу ли я использовать TON Connect без Redis?
A: Нет, Redis необходим для хранения сессий TON Connect.

### Q: Поддерживает ли TON Connect testnet?
A: Да, укажите `TON_NETWORK=testnet` в `.env` и используйте testnet manifest.

### Q: Как отключить TON Connect и использовать только Deep Links?
A: Удалите кнопку "🔗 Подключить кошелек" из клавиатур. TON Connect работает opt-in.

### Q: Безопасно ли хранить session_data в Redis?
A: Да, TON Connect SDK автоматически шифрует критичные данные.

### Q: Можно ли подключить несколько кошельков?
A: Нет, один пользователь - один активный кошелек. Предыдущие сессии деактивируются при подключении нового.

## Дальнейшие улучшения

Потенциальные улучшения:
1. **Webhook вместо polling** - для мгновенного определения подключения
2. **QR код в изображении** - генерация QR кода как картинки
3. **Автоматическое переподключение** - восстановление сессии при перезапуске
4. **Множественные кошельки** - возможность подключить несколько кошельков
5. **Аналитика** - детальная статистика использования TON Connect

## Поддержка

Если возникли вопросы:
1. Проверьте логи: `logs/bot_*.log`
2. Проверьте документацию TON Connect: https://docs.ton.org/develop/dapps/ton-connect
3. GitHub Issues: https://github.com/XaBbl4/pytonconnect

## Лицензия

Эта интеграция использует:
- `pytonconnect` - Python SDK для TON Connect 2.0
- `aiogram` - Async Telegram Bot framework
- `pytoniq` - TON blockchain library

Все библиотеки используются согласно их лицензиям.
