# Структура и логика работы Telegram-бота для розыгрыша призов

## Архитектура проекта

### Технологический стек

- **Python 3.11+** с aiogram 3.x для работы с Telegram Bot API[^1][^2]
- **PostgreSQL** для хранения данных о пользователях, розыгрышах и транзакциях
- **Redis** для кеширования и хранения временных данных
- **YooKassa API** для приема и выплат в рублях[^3]
- **Random.org Signed API** для проверяемого честного розыгрыша[^4][^5]
- **Docker** для развертывания


### Структура репозитория

```
├── app/
│   ├── __init__.py
│   ├── config.py              # Конфигурация (токены, API ключи)
│   ├── database/
│   │   ├── models.py          # SQLAlchemy модели
│   │   ├── crud.py            # CRUD операции
│   │   └── session.py         # Подключение к БД
│   ├── handlers/
│   │   ├── start.py           # Команда /start
│   │   ├── payment.py         # Обработка платежей
│   │   ├── raffle.py          # Логика розыгрыша
│   │   └── admin.py           # Админ-панель
│   ├── services/
│   │   ├── payment_service.py # Интеграция с YooKassa
│   │   ├── random_service.py  # Интеграция с Random.org
│   │   └── notification.py    # Массовые уведомления
│   ├── keyboards/
│   │   └── inline.py          # Клавиатуры
│   └── main.py                # Точка входа
├── migrations/                # Alembic миграции
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
└── .env.example
```


## Решение ключевых проблем

### Проблема 1: Звезды блокируются на 21 день

**Решение**: Использовать систему предоплаченного резерва[^6][^1]

- Держать на балансе бота минимум 5000-10000 звезд "горячего резерва"
- Принимать платежи в звездах от участников
- Выплачивать победителям из резерва немедленно
- Через 21 день полученные звезды становятся доступны и пополняют резерв
- **Важно**: минимум для вывода — 1000 звезд[^7][^8]


### Проблема 2: Автоматические выплаты в рублях

**Решение**: YooKassa API с автоматическими выплатами[^3]

- Подключить YooKassa для приема платежей (карты, СБП)
- Использовать API выплат YooKassa для автоматических переводов
- Поддержка выплат через:
    - Банковские карты
    - СБП (Система быстрых платежей)
    - ЮMoney кошельки
- Для самозанятых — автоматическая регистрация чеков[^3]


### Проблема 3: Массовая рассылка результатов

**Решение**: Асинхронная рассылка с ограничением скорости

- Telegram API позволяет 30 сообщений/секунду
- Использовать aiogram с asyncio для параллельной отправки
- Реализовать очередь уведомлений через Redis
- Отправлять результаты в порядке: победитель → участники → наблюдатели


## Пошаговый план реализации

### Этап 1: Подготовка (День 1)

**Создание репозитория**

- Создать GitHub репозиторий
- Инициализировать структуру проекта
- Настроить .gitignore для Python
- Создать .env.example с переменными окружения

**Регистрация сервисов**

- Создать бота через [@BotFather](https://t.me/BotFather)
- Зарегистрироваться в [YooKassa](https://yookassa.ru/) (для рублей)
- Получить API ключ на [Random.org](https://api.random.org/)
- Настроить PostgreSQL (можно через Docker)


### Этап 2: Базовая инфраструктура (День 1-2)

**База данных**

```python
# models.py - основные таблицы:
- users (id, telegram_id, username, balance_stars, balance_rub, created_at)
- raffles (id, min_participants, entry_fee_type, entry_fee_amount, 
           status, started_at, finished_at, winner_id, random_result)
- participants (id, raffle_id, user_id, joined_at, transaction_id)
- transactions (id, user_id, type, amount, currency, status, 
                payment_id, created_at)
```

**Docker setup**

```yaml
# docker-compose.yml
services:
  bot:
    build: .
    env_file: .env
  postgres:
    image: postgres:16
  redis:
    image: redis:7
```


### Этап 3: Платежная система (День 2-3)

**Прием платежей в звездах**

```python
# Telegram Bot API встроенная поддержка Stars
# В handlers/payment.py:
- Создать invoice через sendInvoice с provider_token=""
- Обработать pre_checkout_query
- Обработать successful_payment
- Записать транзакцию в БД
```

**Прием платежей в рублях**

```python
# services/payment_service.py - интеграция YooKassa:
- Создать платеж через YooKassa API
- Сгенерировать платежную ссылку
- Настроить webhook для уведомлений об оплате
- Обработать успешный платеж
```


### Этап 4: Логика розыгрыша (День 3-4)

**Механика розыгрыша**

```python
# services/raffle.py:

1. Старт розыгрыша:
   - Объявить условия (взнос, минимум участников)
   - Установить таймер автостарта (например, 24 часа)

2. Прием участников:
   - Проверить платеж
   - Добавить в список участников
   - Уведомить о присоединении

3. Запуск розыгрыша (при достижении минимума):
   - Собрать список participant_ids
   - Вызвать Random.org API
   - Получить подписанный результат
   - Сохранить signature для проверки
   
4. Определение победителя:
   - Взять случайное число от Random.org
   - Выбрать победителя по индексу
   - Рассчитать суммы (приз = взнос * участников - комиссия)
```

**Интеграция Random.org**

```python
# services/random_service.py:
import requests

def get_signed_random(min_val, max_val, api_key):
    """Получить проверяемое случайное число"""
    payload = {
        "jsonrpc": "2.0",
        "method": "generateSignedIntegers",
        "params": {
            "apiKey": api_key,
            "n": 1,
            "min": min_val,
            "max": max_val
        },
        "id": 1
    }
    response = requests.post(
        "https://api.random.org/json-rpc/4/invoke",
        json=payload
    )
    return response.json()  # Содержит число + signature
```


### Этап 5: Выплаты (День 4-5)

**Выплата в звездах**

```python
# Использовать Telegram refundStarPayment API
# Можно возвращать звезды напрямую
# Либо использовать резервный фонд для мгновенных выплат

async def payout_stars(user_id: int, amount: int):
    # Создать инвойс наоборот - от бота к пользователю
    # Использовать резервный фонд
    # Записать транзакцию
```

**Выплата в рублях**

```python
# services/payment_service.py - YooKassa Payouts API:

async def payout_rubles(card_number: str, amount: float):
    """Выплата через YooKassa API"""
    payload = {
        "amount": {"value": str(amount), "currency": "RUB"},
        "payout_destination_data": {
            "type": "bank_card",
            "card": {"number": card_number}
        }
    }
    # Или через СБП по номеру телефона
```


### Этап 6: Система уведомлений (День 5)

**Массовая рассылка**

```python
# services/notification.py:

async def notify_all_participants(raffle_id: int, message: str):
    """Асинхронная рассылка с rate limiting"""
    participants = await get_raffle_participants(raffle_id)
    
    # Батчами по 30 сообщений в секунду
    for batch in chunks(participants, 30):
        tasks = [
            bot.send_message(user.telegram_id, message)
            for user in batch
        ]
        await asyncio.gather(*tasks, return_exceptions=True)
        await asyncio.sleep(1)  # Пауза между батчами
```

**Сценарии уведомлений**

- Новый участник присоединился → всем текущим участникам
- Начался розыгрыш → всем участникам
- Объявление победителя → персональное победителю + общее всем
- Выплата произведена → победителю


### Этап 7: Интерфейс бота (День 6)

**Основные команды**

```python
/start - Приветствие + главное меню
/balance - Показать баланс звезд/рублей
/join_raffle - Присоединиться к текущему розыгрышу
/my_raffles - История участия
/admin - Админ-панель (только для владельца)
```

**Inline-клавиатуры**

```python
# keyboards/inline.py:

main_menu = [
    ["💫 Присоединиться (Stars)", "💳 Присоединиться (RUB)"],
    ["📊 Статистика", "📜 Правила"],
    ["💰 Мой баланс", "🏆 История"]
]

payment_choice = [
    ["⭐ Оплатить звездами (10 stars)"],
    ["💳 Оплатить рублями (100 RUB)"],
    ["◀️ Назад"]
]
```


### Этап 8: Админ-панель (День 6-7)

**Функции админа**

```python
# handlers/admin.py:

- Создать новый розыгрыш (взнос, минимум участников, дедлайн)
- Остановить розыгрыш вручную
- Запустить розыгрыш принудительно
- Просмотр всех участников
- Статистика по выплатам
- Управление резервным фондом звезд
- Настройка комиссии (%)
```


### Этап 9: Безопасность и валидация (День 7)

**Критичные проверки**

```python
# Перед выплатой:
- Проверить достаточность средств на балансе
- Валидировать signature от Random.org
- Проверить статус транзакций
- Защита от двойной выплаты (idempotency)

# Антифрод:
- Лимит на количество участий в день
- Проверка дубликатов платежей
- Логирование всех операций
```

**Обработка ошибок**

```python
try:
    # Выплата
except PaymentError as e:
    # Откатить транзакцию
    # Вернуть средства
    # Уведомить админа
    # Записать в лог
```


### Этап 10: Тестирование (День 7-8)

**Unit-тесты**

- Тестирование расчета призового фонда
- Валидация Random.org результатов
- Проверка логики выплат

**Интеграционные тесты**

- Полный цикл розыгрыша с тестовыми платежами
- Проверка webhook'ов
- Тестирование массовой рассылки

**Тестовый запуск**

- Создать тестовый розыгрыш с минимальными суммами
- Пригласить 5-10 тестовых пользователей
- Провести полный цикл


### Этап 11: Развертывание (День 8)

**Подготовка сервера**

```bash
# На VPS (например, DigitalOcean, Timeweb):
1. Установить Docker и Docker Compose
2. Клонировать репозиторий
3. Настроить .env с продакшн-ключами
4. Запустить docker-compose up -d
```

**Мониторинг**

```python
# Добавить логирование:
- Winston/Loguru для структурированных логов
- Sentry для отслеживания ошибок
- Prometheus + Grafana для метрик (опционально)
```

**Резервное копирование**

```bash
# Настроить автоматический бэкап PostgreSQL:
pg_dump -h localhost -U postgres raffle_bot > backup_$(date +%Y%m%d).sql
# Через cron каждые 6 часов
```


### Этап 12: Релиз (День 9)

**Финальные проверки**

- [ ] Все платежи работают (звезды + рубли)
- [ ] Выплаты проходят корректно
- [ ] Массовая рассылка функционирует
- [ ] Random.org возвращает проверяемые результаты
- [ ] Логи пишутся правильно
- [ ] Резервный фонд звезд достаточен

**Запуск**

- Объявить о запуске в канале/группе
- Создать первый розыгрыш
- Мониторить первые 24 часа активно
- Собирать обратную связь


## Рекомендации по комиссии

**Для звезд**

- Взнос: 10 stars (≈\$0.13)
- Комиссия: 15-20% (прибыль + покрытие резерва)
- Минимум участников: 10 человек
- Призовой фонд: 80-85 stars

**Для рублей**

- Взнос: 100-300 рублей
- Комиссия: 10-15% (комиссия YooKassa ~3% + прибыль)
- Минимум участников: 10 человек
- Призовой фонд: 85-90% от общей суммы


## Критичные моменты

**Управление резервом звезд**

- Первоначальный резерв: 5000-10000 stars (~\$65-130)
- Пополнять при падении ниже 3000 stars
- Мониторить баланс ежедневно
- Автоматические уведомления при низком балансе

**Правовые аспекты**

- Оформить как "развлекательная игра", а не азартная
- Не использовать термины "лотерея", "казино"
- Публичная верификация честности через Random.org
- Прозрачные правила участия

**Прозрачность**

- Публиковать ссылку на Random.org signature
- Показывать расчет призового фонда
- Отображать комиссию явно
- Сохранять историю всех розыгрышей

Этот план обеспечивает быструю и надежную реализацию бота за 8-9 дней с упором на стабильность платежной системы и прозрачность розыгрышей.[^2][^1][^4][^3]

